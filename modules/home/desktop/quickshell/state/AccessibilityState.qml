import QtQuick

QtObject {
    // Phase 1 default. Wire portal Settings changes here when its interface is
    // available; every component already consumes this one state value.
    property bool reducedMotion: false
}
